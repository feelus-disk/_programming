<%= @comment.to_s.gsub(/^/,"* ") %>
*
*  Electroweak parameters
*

C... Taken from PDG-2012

&EWPars
  ! Choice of EW scheme: 0 - alpha(0), 2 - G_mu, 4 - alpha(mz)
  gfscheme		= <%=  @gfscheme %>

  ! scales. -1d0 sets to invariant mass of products (sqspr for hard)
  fscale                = <%= "%.15g" % @fscale %>
  rscale                = <%= "%.15g" % @rscale %>
  ome                   = <%= "%.15g" % @ome %>

  alpha 		= <%= "%.15g" % @alpha %>
  gf			= <%= "%.15g" % @gf %>
  alphas                = <%= "%.15g" % @alphas %>
  conhc 		= <%= "%.15g" % @conhc %>

  ! boson masses
  mw			= <%= "%.15g" % @mw %>
  mz			= <%= "%.15g" % @mz %>
  mh			= <%= "%.15g" % @mh %>
  ma                    = <%= "%.15g" % @ma %>
  mv                    = <%= "%.15g" % @mv %>

  ! widths
  wz			= <%= "%.15g" % @wz %>
  ww			= <%= "%.15g" % @ww %>
  wh			= <%= "%.15g" % @wh %>
  wtp			= <%= "%.15g" % @wtp %>

  ! CKM
  Vud			= <%= "%.15g" % @vud %>
  Vus			= <%= "%.15g" % @vus %>
  Vub			= <%= "%.15g" % @vub %>
  Vcd			= <%= "%.15g" % @vcd %>
  Vcs			= <%= "%.15g" % @vcs %>
  Vcb			= <%= "%.15g" % @vcb %>

  !*** fermion masses

  ! Lepton masses
  men			= <%= "%.15g" % @men %>
  mel			= <%= "%.15g" % @mel %>
  mmn			= <%= "%.15g" % @mmn %>
  mmo			= <%= "%.15g" % @mmo %>
  mtn			= <%= "%.15g" % @mtn %>
  mta			= <%= "%.15g" % @mta %>

  ! Quark masses:
  ! masses of four light quarks are taken from 0911.2329v2
  ! all other parameters are from PDG 2011 (on 16/05/2012)
  mdn			= <%= "%.15g" % @mdn %>
  mup			= <%= "%.15g" % @mup %>
  mst			= <%= "%.15g" % @mst %>
  mch			= <%= "%.15g" % @mch %>
  mbt			= <%= "%.15g" % @mbt %>
  mtp			= <%= "%.15g" % @mtp %>

  ! rmf1 fermion mass array, for technical use
  rmf1 = <%= @rmf1.map{|e| "%.15g" % e}*", " %>
   
/
