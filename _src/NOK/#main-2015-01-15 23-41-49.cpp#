#include <stdio.h>
#include <vector>

int main()
{
  int _case=0;
  while(true) {
    _case++;
    int N;
    scanf("%d\n",&N);
    if(!N) return 0;
    int s=0;
    int n=0;
    for(int d=2; d<=N; d++) {
      int m=1;
      while(N%d==0) {
	N/=d;
	m*=d;
      }
      
    }
  }
}
}