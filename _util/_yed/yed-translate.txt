file -> export -> save
Image export
	clipping//обрезание
		size
			original size
			custom height
			custom width
		custom height
		custom width
		margin//край, запас
		region
			complete diagram
			visible region
		scaling factor//масштаб
	image
		antialiasing//сглаживание
		paint selection marker//рисовать выделенное маркером
	tiling//покрытие
		activate tiling//включить плитку
		tiles per row
		tiles per coloumn
		generate HTML table
printing options
	general
		clip area
			entire graph
			view only
		scaling
			automatic
			user defined
		scaling factor
		poster rows
		poster coloumns
		add poster coords
		print centered
	title
		text
		titlebar color
		text color
		font size
		font name
		show on each page
	footer
		text
		background color
		text color
		font size
		font name
		show on each page
preferences
	general
		show welcome screen
		check for new version on startup
		tooltips
			specify display duration
			no tooltips
			infinite display duration
		tooltips display duration (seconds)
		language
			english
			german
		number of steps of undu
		animated zooming
		mouse wheel behavior
			zoom in/out
			[wheel] up/down [shift wheel] left/right [ctrl+wheel] in/out
			disabled
		fit content on resize
		maximum number of current elements
	display
		antialiasing
		drow selected elements on top
		edge path clip
		paint nodes over edges
		paint details threshold
		bridge stile
		bridge scaling
		crossing determination mode
		group state icon
			show group state except on export
			never show group state
			always show group state
		context views update trigger
			selection trigger
			hover trigger
		hierarchy aware context views
	editor
		create node on background click
		synchronize open and closed group label
		center edge anchors
		select dropped node/edge
		split edge on node drop
		create default label for new nodes
		edit label on create node
		edit label on create edge
		dynamically adjust node size to label size
		remove bends inside a node
		show scrollbars in edit mode
		label editor
			[enter] closes editor [shift enter] creates new line
			[enter] creates new line [shift enter] closes editor
		resize from center
	background
		mode
			plain
			stretched
			tiled
			bricked
			centered
			dynamic
		color
		image
	modules
		morph layouts
		morph layout behavior
			fit content
			keep focus
		morph durations (ms)
		show modules dialog
	snap lines
		color
		node to node distance
		node to edge distance
		edge to edge distance
		consider edge distance when moving ports
	grid
		color
		spacing
		type
select elements
	general
		act on current selection only
		select inverse
		clear selection firsrt
	nodes
		use this criteria
		select
			all
			label
			fill color
			degree
			indegree
			outdegree
			sources of selected edges
			targets of selected edges
			neighbors of selected nodes
			successors of selected nodes
			predcessors of selected nodes
			folder nodes
			group nodes
			children of selected nodes
			parents of selected nodes
		label
		match type
			complete
			suffix match
			prefix match
			infix match
		case sensitive
		minimal degree
		maximal degree
		fill color
		maximal path distance
	edges
		use this criteria
		select
...
		label
		match type
			complete
			suffix match
			prefix match
			infix match
		case sensitive
		line color
	bends
	node labels
	edge labels