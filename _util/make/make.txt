# GNU Make 4.0
# Эта программа собрана для i686-pc-cygwin
# Copyright (C) 1988-2013 Free Software Foundation, Inc.
# Лицензия GPLv3+: GNU GPL версии 3 или новее <http://gnu.org/licenses/gpl.html>
# Это свободное программное обеспечение: вы можете свободно изменять его и
# распространять. НЕТ НИКАКИХ ГАРАНТИЙ вне пределов, допустимых законом.

# База данных Make, напечатана Wed Oct 22 12:05:32 2014

# Переменные

# Директива «override»
GNUMAKEFLAGS := 
# автоматическая
<D = $(patsubst %/,%,$(dir $<))
# автоматическая
?F = $(notdir $?)
# определена в среде
AMDAPPSDKROOT = C:\Program Files\AMD APP\
# по умолчанию
.SHELLFLAGS := -c
# make-файл
MAKEFLAGS = p
# по умолчанию
CWEAVE = cweave
# автоматическая
?D = $(patsubst %/,%,$(dir $?))
# определена в среде
!:: = ::\
# автоматическая
@D = $(patsubst %/,%,$(dir $@))
# определена в среде
HOMEDRIVE = C:
# автоматическая
@F = $(notdir $@)
# автоматическая
^D = $(patsubst %/,%,$(dir $^))
# make-файл
CURDIR := /home/Acer W500
# make-файл
SHELL = /bin/sh
# по умолчанию
RM = rm -f
# по умолчанию
CO = co
# определена в среде
WINDIR = C:\Windows
# определена в среде
FP_NO_HOST_CHECK = NO
# определена в среде
TZ = Europe/Moscow
# определена в среде
_ = /usr/bin/make
# по умолчанию
PREPROCESS.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -F
# определена в среде
EXECIGNORE = *.dll
# определена в среде
TouchAppsTargetDir = C:\Program Files\Microsoft Touch Pack for Windows 7\
# по умолчанию
LINK.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# по умолчанию
LINK.o = $(CC) $(LDFLAGS) $(TARGET_ARCH)
# по умолчанию
OUTPUT_OPTION = -o $@
# по умолчанию
COMPILE.cpp = $(COMPILE.cc)
# определена в среде
PATHEXT = .COM;.EXE;.BAT;.CMD;.VBS;.VBE;.JS;.JSE;.WSF;.WSH;.MSC
# make-файл
MAKEFILE_LIST := 
# определена в среде
USERNAME = Acer W500
# по умолчанию
LINK.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# по умолчанию
CC = cc
# определена в среде
WIPFC = D:\WATCOM\WIPFC
# определена в среде
INFOPATH = /usr/local/info:/usr/share/info:/usr/info
# по умолчанию
CHECKOUT,v = +$(if $(wildcard $@),,$(CO) $(COFLAGS) $< $@)
# по умолчанию
LEX.m = $(LEX) $(LFLAGS) -t
# по умолчанию
CPP = $(CC) -E
# по умолчанию
LINK.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# по умолчанию
MAKE_HOST := i686-pc-cygwin
# определена в среде
PATH = /usr/local/bin:/usr/bin:/cygdrive/d/WATCOM/BINNT:/cygdrive/d/WATCOM/BINW:/cygdrive/c/Program Files/Common Files/Microsoft Shared/Windows Live:/cygdrive/c/Program Files/AMD APP/bin/x86:/cygdrive/c/Windows/system32:/cygdrive/c/Windows:/cygdrive/c/Windows/System32/Wbem:/cygdrive/c/Windows/System32/WindowsPowerShell/v1.0:/cygdrive/c/Program Files/ATI Technologies/ATI.ACE/Core-Static:/cygdrive/c/Program Files/Windows Live/Shared:/cygdrive/d/Program Files/MATLAB/R2008b/bin:/cygdrive/d/Program Files/MATLAB/R2008b/bin/win32:/cygdrive/c/Program Files/Microsoft SQL Server/90/Tools/binn:/cygdrive/d/desktop/mybin:/cygdrive/d/Program Files/MiKTeX 2.9/miktex/bin
# по умолчанию
LD = ld
# по умолчанию
TEXI2DVI = texi2dvi
# по умолчанию
YACC = yacc
# определена в среде
WHTMLHELP = D:\WATCOM\BINNT\HELP
# по умолчанию
COMPILE.mod = $(M2C) $(M2FLAGS) $(MODFLAGS) $(TARGET_ARCH)
# определена в среде
PROCESSOR_ARCHITECTURE = x86
# по умолчанию
LINK.r = $(FC) $(FFLAGS) $(RFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# определена в среде
USERPROFILE = C:\Users\Acer W500
# по умолчанию
LINT = lint
# по умолчанию
COMPILE.f = $(FC) $(FFLAGS) $(TARGET_ARCH) -c
# по умолчанию
LINT.c = $(LINT) $(LINTFLAGS) $(CPPFLAGS) $(TARGET_ARCH)
# по умолчанию
YACC.m = $(YACC) $(YFLAGS)
# определена в среде
ALLUSERSPROFILE = C:\ProgramData
# по умолчанию
YACC.y = $(YACC) $(YFLAGS)
# определена в среде
PGROUPD_LICENSE_FILE = D:\Program Files\PGI\license.dat
# по умолчанию
AR = ar
# определена в среде
NUMBER_OF_PROCESSORS = 2
# по умолчанию
.FEATURES := target-specific order-only second-expansion else-if shortest-stem undefine oneshell archives jobserver output-sync check-symlink load
# по умолчанию
TANGLE = tangle
# по умолчанию
GET = get
# автоматическая
%F = $(notdir $%)
# определена в среде
windows_tracing_flags = 3
# определена в среде
EDPATH = D:\WATCOM\EDDAT
# по умолчанию
COMPILE.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# по умолчанию
CTANGLE = ctangle
# по умолчанию
.LIBPATTERNS = lib%.so lib%.a
# определена в среде
ORIGINAL_PATH = /cygdrive/d/WATCOM/BINNT:/cygdrive/d/WATCOM/BINW:/cygdrive/c/Program Files/Common Files/Microsoft Shared/Windows Live:/cygdrive/c/Program Files/AMD APP/bin/x86:/cygdrive/c/Windows/system32:/cygdrive/c/Windows:/cygdrive/c/Windows/System32/Wbem:/cygdrive/c/Windows/System32/WindowsPowerShell/v1.0:/cygdrive/c/Program Files/ATI Technologies/ATI.ACE/Core-Static:/cygdrive/c/Program Files/Windows Live/Shared:/cygdrive/d/Program Files/MATLAB/R2008b/bin:/cygdrive/d/Program Files/MATLAB/R2008b/bin/win32:/cygdrive/c/Program Files/Microsoft SQL Server/90/Tools/binn:/cygdrive/d/desktop/mybin:/cygdrive/d/Program Files/MiKTeX 2.9/miktex/bin
# определена в среде
COMPUTERNAME = ACERW500-ПК
# по умолчанию
LINK.C = $(LINK.cc)
# определена в среде
PWD = /home/Acer W500
# определена в среде
PROGRAMFILES = C:\Program Files
# по умолчанию
LINK.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_MACH)
# по умолчанию
PREPROCESS.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -F
# по умолчанию
LINK.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# определена в среде
PUBLIC = C:\Users\Public
# по умолчанию
LINK.s = $(CC) $(ASFLAGS) $(LDFLAGS) $(TARGET_MACH)
# определена в среде
HOME = /home/Acer W500
# определена в среде
ProgramData = C:\ProgramData
# определена в среде
VS90COMNTOOLS = D:\Program Files\Microsoft Visual Studio 9.0\Common7\Tools\
# определена в среде
FINCLUDE = D:\WATCOM\SRC\FORTRAN;D:\WATCOM\SRC\FORTRAN\WIN
# определена в среде
PRINTER = EPSON L210 Series
# определена в среде
COMMONPROGRAMFILES = C:\Program Files\Common Files
# определена в среде
LOGONSERVER = \\ACERW500-ПК
# определена в среде
USERDOMAIN = AcerW500-ПК
# определена в среде
HOSTNAME = AcerW500-��
# определена в среде
MAKELEVEL := 0
# по умолчанию
COMPILE.m = $(OBJC) $(OBJCFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# по умолчанию
MAKE = $(MAKE_COMMAND)
# определена в среде
SHLVL = 1
# по умолчанию
AS = as
# по умолчанию
PREPROCESS.S = $(CC) -E $(CPPFLAGS)
# по умолчанию
COMPILE.p = $(PC) $(PFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# по умолчанию
MAKE_VERSION := 4.0
# определена в среде
USER = Acer W500
# по умолчанию
FC = f77
# make-файл
.DEFAULT_GOAL := 
# определена в среде
INCLUDE = D:\WATCOM\H;D:\WATCOM\H\NT;D:\WATCOM\H\NT\DIRECTX;D:\WATCOM\H\NT\DDK
# автоматическая
%D = $(patsubst %/,%,$(dir $%))
# определена в среде
LOCALAPPDATA = C:\Users\Acer W500\AppData\Local
# по умолчанию
ARFLAGS = rv
# по умолчанию
WEAVE = weave
# по умолчанию
MAKE_COMMAND := make
# определена в среде
PROFILEREAD = true
# по умолчанию
LINK.cpp = $(LINK.cc)
# по умолчанию
F77 = $(FC)
# определена в среде
OLDPWD = /cygdrive/d/desktop
# по умолчанию
.VARIABLES := 
# по умолчанию
PC = pc
# автоматическая
*F = $(notdir $*)
# определена в среде
PROCESSOR_IDENTIFIER = x86 Family 20 Model 2 Stepping 0, AuthenticAMD
# определена в среде
OS = Windows_NT
# по умолчанию
COMPILE.def = $(M2C) $(M2FLAGS) $(DEFFLAGS) $(TARGET_ARCH)
# по умолчанию
LEX = lex
# определена в среде
HOMEPATH = \Users\Acer W500
# определена в среде
MFLAGS = -p
# автоматическая
*D = $(patsubst %/,%,$(dir $*))
# по умолчанию
LEX.l = $(LEX) $(LFLAGS) -t
# определена в среде
PSModulePath = C:\Windows\system32\WindowsPowerShell\v1.0\Modules\
# определена в среде
SYSTEMROOT = C:\Windows
# автоматическая
+D = $(patsubst %/,%,$(dir $+))
# по умолчанию
COMPILE.r = $(FC) $(FFLAGS) $(RFLAGS) $(TARGET_ARCH) -c
# автоматическая
+F = $(notdir $+)
# по умолчанию
M2C = m2c
# определена в среде
APPDATA = C:\Users\Acer W500\AppData\Roaming
# определена в среде
TEMP = /tmp
# определена в среде
COMSPEC = C:\Windows\system32\cmd.exe
# по умолчанию
MAKEFILES := 
# по умолчанию
COMPILE.cc = $(CXX) $(CXXFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# автоматическая
<F = $(notdir $<)
# определена в среде
PS1 = \[\e]0;\w\a\]\n\[\e[32m\]\u@\h \[\e[33m\]\w\[\e[0m\]\n\$ 
# по умолчанию
CXX = g++
# по умолчанию
COFLAGS = 
# по умолчанию
COMPILE.C = $(COMPILE.cc)
# автоматическая
^F = $(notdir $^)
# по умолчанию
COMPILE.S = $(CC) $(ASFLAGS) $(CPPFLAGS) $(TARGET_MACH) -c
# по умолчанию
LINK.F = $(FC) $(FFLAGS) $(CPPFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# по умолчанию
SUFFIXES := .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
# определена в среде
SYSTEMDRIVE = C:
# по умолчанию
COMPILE.c = $(CC) $(CFLAGS) $(CPPFLAGS) $(TARGET_ARCH) -c
# определена в среде
SESSIONNAME = Console
# определена в среде
PROCESSOR_REVISION = 0200
# определена в среде
WATCOM = D:\WATCOM
# по умолчанию
COMPILE.s = $(AS) $(ASFLAGS) $(TARGET_MACH)
# по умолчанию
.INCLUDE_DIRS = /usr/include /usr/include
# определена в среде
TMP = /tmp
# по умолчанию
.RECIPEPREFIX := 
# по умолчанию
MAKEINFO = makeinfo
# по умолчанию
OBJC = cc
# по умолчанию
TEX = tex
# определена в среде
LANG = ru_RU.UTF-8
# определена в среде
PROCESSOR_LEVEL = 20
# определена в среде
TERM = xterm
# по умолчанию
F77FLAGS = $(FFLAGS)
# по умолчанию
LINK.f = $(FC) $(FFLAGS) $(LDFLAGS) $(TARGET_ARCH)
# определена в среде
windows_tracing_logfile = C:\BVTBin\Tests\installpackage\csilogfile.log
# определена в среде
VBOX_INSTALL_PATH = D:\Program Files\Oracle\VirtualBox\
# состояние переменных в хеш-таблице:
# Загружено=157/1024=15%, Переформулировано=0, Противоречий=26/177=15%

# Значения переменных особенные для маски

# Нет значений переменных особенных для маски.

# Каталоги

# SCCS: невозможно получить сведения вызовом stat.
# . (устройство -1228164935, inode 247468): 10 файлов,57 недостижимых целей.
# RCS: невозможно получить сведения вызовом stat.

# 10 файлов,57 недостижимых целей в 3 каталогах.

# Неявные правила

%.out:

%.a:

%.ln:

%.o:

%: %.o
#  способ, который следует применить (встроенные):
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.c:

%: %.c
#  способ, который следует применить (встроенные):
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.ln: %.c
#  способ, который следует применить (встроенные):
	$(LINT.c) -C$* $<

%.o: %.c
#  способ, который следует применить (встроенные):
	$(COMPILE.c) $(OUTPUT_OPTION) $<

%.cc:

%: %.cc
#  способ, который следует применить (встроенные):
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cc
#  способ, который следует применить (встроенные):
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

%.C:

%: %.C
#  способ, который следует применить (встроенные):
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.C
#  способ, который следует применить (встроенные):
	$(COMPILE.C) $(OUTPUT_OPTION) $<

%.cpp:

%: %.cpp
#  способ, который следует применить (встроенные):
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.cpp
#  способ, который следует применить (встроенные):
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

%.p:

%: %.p
#  способ, который следует применить (встроенные):
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.p
#  способ, который следует применить (встроенные):
	$(COMPILE.p) $(OUTPUT_OPTION) $<

%.f:

%: %.f
#  способ, который следует применить (встроенные):
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.f
#  способ, который следует применить (встроенные):
	$(COMPILE.f) $(OUTPUT_OPTION) $<

%.F:

%: %.F
#  способ, который следует применить (встроенные):
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.F
#  способ, который следует применить (встроенные):
	$(COMPILE.F) $(OUTPUT_OPTION) $<

%.f: %.F
#  способ, который следует применить (встроенные):
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

%.m:

%: %.m
#  способ, который следует применить (встроенные):
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.m
#  способ, который следует применить (встроенные):
	$(COMPILE.m) $(OUTPUT_OPTION) $<

%.r:

%: %.r
#  способ, который следует применить (встроенные):
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.r
#  способ, который следует применить (встроенные):
	$(COMPILE.r) $(OUTPUT_OPTION) $<

%.f: %.r
#  способ, который следует применить (встроенные):
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

%.y:

%.ln: %.y
#  способ, который следует применить (встроенные):
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

%.c: %.y
#  способ, который следует применить (встроенные):
	$(YACC.y) $< 
	 mv -f y.tab.c $@

%.l:

%.ln: %.l
#  способ, который следует применить (встроенные):
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

%.c: %.l
#  способ, который следует применить (встроенные):
	@$(RM) $@ 
	 $(LEX.l) $< > $@

%.r: %.l
#  способ, который следует применить (встроенные):
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

%.ym:

%.m: %.ym
#  способ, который следует применить (встроенные):
	$(YACC.m) $< 
	 mv -f y.tab.c $@

%.yl:

%.s:

%: %.s
#  способ, который следует применить (встроенные):
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.s
#  способ, который следует применить (встроенные):
	$(COMPILE.s) -o $@ $<

%.S:

%: %.S
#  способ, который следует применить (встроенные):
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

%.o: %.S
#  способ, который следует применить (встроенные):
	$(COMPILE.S) -o $@ $<

%.s: %.S
#  способ, который следует применить (встроенные):
	$(PREPROCESS.S) $< > $@

%.mod:

%: %.mod
#  способ, который следует применить (встроенные):
	$(COMPILE.mod) -o $@ -e $@ $^

%.o: %.mod
#  способ, который следует применить (встроенные):
	$(COMPILE.mod) -o $@ $<

%.sym:

%.def:

%.sym: %.def
#  способ, который следует применить (встроенные):
	$(COMPILE.def) -o $@ $<

%.h:

%.info:

%.dvi:

%.tex:

%.dvi: %.tex
#  способ, который следует применить (встроенные):
	$(TEX) $<

%.texinfo:

%.info: %.texinfo
#  способ, который следует применить (встроенные):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texinfo
#  способ, который следует применить (встроенные):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.texi:

%.info: %.texi
#  способ, который следует применить (встроенные):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.texi
#  способ, который следует применить (встроенные):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.txinfo:

%.info: %.txinfo
#  способ, который следует применить (встроенные):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

%.dvi: %.txinfo
#  способ, который следует применить (встроенные):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

%.w:

%.c: %.w
#  способ, который следует применить (встроенные):
	$(CTANGLE) $< - $@

%.tex: %.w
#  способ, который следует применить (встроенные):
	$(CWEAVE) $< - $@

%.ch:

%.web:

%.p: %.web
#  способ, который следует применить (встроенные):
	$(TANGLE) $<

%.tex: %.web
#  способ, который следует применить (встроенные):
	$(WEAVE) $<

%.sh:

%: %.sh
#  способ, который следует применить (встроенные):
	cat $< >$@ 
	 chmod a+x $@

%.elc:

%.el:

(%): %
#  способ, который следует применить (встроенные):
	$(AR) $(ARFLAGS) $@ $<

%.out: %
#  способ, который следует применить (встроенные):
	@rm -f $@ 
	 cp $< $@

%.c: %.w %.ch
#  способ, который следует применить (встроенные):
	$(CTANGLE) $^ $@

%.tex: %.w %.ch
#  способ, который следует применить (встроенные):
	$(CWEAVE) $^ $@

%:: %,v
#  способ, который следует применить (встроенные):
	$(CHECKOUT,v)

%:: RCS/%,v
#  способ, который следует применить (встроенные):
	$(CHECKOUT,v)

%:: RCS/%
#  способ, который следует применить (встроенные):
	$(CHECKOUT,v)

%:: s.%
#  способ, который следует применить (встроенные):
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

%:: SCCS/s.%
#  способ, который следует применить (встроенные):
	$(GET) $(GFLAGS) $(SCCS_OUTPUT_OPTION) $<

# Неявных правил: 92, терминальных: 5 (5,4%) терминал.

# Файлы

# Не является целью:
.web.p:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(TANGLE) $<

# Не является целью:
.l.r:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LEX.l) $< > $@ 
	 mv -f lex.yy.r $@

# Не является целью:
.dvi:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.ym:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.f.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.f) $(OUTPUT_OPTION) $<

# Не является целью:
.l:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.m:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.m) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.ln:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.o) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.y:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.def.sym:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.def) -o $@ $<

# Не является целью:
.p.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.p) $(OUTPUT_OPTION) $<

# Не является целью:
.p:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.p) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.txinfo.dvi:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# Не является целью:
.a:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.yl:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.l.ln:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	@$(RM) $*.c
	 $(LEX.l) $< > $*.c
	$(LINT.c) -i $*.c -o $@
	 $(RM) $*.c

# Не является целью:
.F.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.F) $(OUTPUT_OPTION) $<

# Не является целью:
.texi.info:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# Не является целью:
.w.c:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(CTANGLE) $< - $@

# Не является целью:
.texi.dvi:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# Не является целью:
.ch:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.m.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.m) $(OUTPUT_OPTION) $<

# Не является целью:
.cc:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.cc) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.cc.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.cc) $(OUTPUT_OPTION) $<

# Не является целью:
.def:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.SUFFIXES: .out .a .ln .o .c .cc .C .cpp .p .f .F .m .r .y .l .ym .yl .s .S .mod .sym .def .h .info .dvi .tex .texinfo .texi .txinfo .w .ch .web .sh .elc .el
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.c.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.c) $(OUTPUT_OPTION) $<

# Не является целью:
Makefile:
#  Производился поиск неявных правил.
#  Файл не существует.
#  Файл был обновлён.
#  Попытка обновления безуспешна.

# Не является целью:
.r.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.r) $(OUTPUT_OPTION) $<

# Не является целью:
.r:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.r) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.ym.m:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(YACC.m) $< 
	 mv -f y.tab.c $@

# Не является целью:
.y.ln:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(YACC.y) $< 
	 $(LINT.c) -C$* y.tab.c 
	 $(RM) y.tab.c

# Не является целью:
makefile:
#  Производился поиск неявных правил.
#  Файл не существует.
#  Файл был обновлён.
#  Попытка обновления безуспешна.

# Не является целью:
.elc:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.l.c:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	@$(RM) $@ 
	 $(LEX.l) $< > $@

# Не является целью:
.out:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.C:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.C) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.r.f:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(PREPROCESS.r) $(OUTPUT_OPTION) $<

# Не является целью:
.S:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.S) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.texinfo.info:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# Не является целью:
.c:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.c) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.w.tex:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(CWEAVE) $< - $@

# Не является целью:
.c.ln:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINT.c) -C$* $<

# Не является целью:
.s.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.s) -o $@ $<

# Не является целью:
.s:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.s) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.texinfo.dvi:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(TEXI2DVI) $(TEXI2DVI_FLAGS) $<

# Не является целью:
.el:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.lm.m:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	@$(RM) $@ 
	 $(LEX.m) $< > $@

# Не является целью:
.y.c:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(YACC.y) $< 
	 mv -f y.tab.c $@

# Не является целью:
.web.tex:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(WEAVE) $<

# Не является целью:
.texinfo:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.DEFAULT:
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.h:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.tex.dvi:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(TEX) $<

# Не является целью:
.cpp.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.cpp) $(OUTPUT_OPTION) $<

# Не является целью:
.cpp:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.cpp) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.C.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.C) $(OUTPUT_OPTION) $<

# Не является целью:
.texi:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.txinfo:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.tex:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.txinfo.info:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(MAKEINFO) $(MAKEINFO_FLAGS) $< -o $@

# Не является целью:
GNUmakefile:
#  Производился поиск неявных правил.
#  Файл не существует.
#  Файл был обновлён.
#  Попытка обновления безуспешна.

# Не является целью:
.sh:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	cat $< >$@ 
	 chmod a+x $@

# Не является целью:
.S.s:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(PREPROCESS.S) $< > $@

# Не является целью:
.mod:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.mod) -o $@ -e $@ $^

# Не является целью:
.mod.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.mod) -o $@ $<

# Не является целью:
.F.f:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(PREPROCESS.F) $(OUTPUT_OPTION) $<

# Не является целью:
.w:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.S.o:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(COMPILE.S) -o $@ $<

# Не является целью:
.F:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.F) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.web:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.sym:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# Не является целью:
.f:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.
#  способ, который следует применить (встроенные):
	$(LINK.f) $^ $(LOADLIBES) $(LDLIBS) -o $@

# Не является целью:
.info:
# Встроенное правило
#  Поиск неявных правил не производился.
#  Время изменения ни разу не проверялось.
#  Файл не был обновлён.

# состояние файлов хеш-таблицы:
# Загружено=75/1024=7%, Переформулировано=0, Противоречий=447/1785=25%
# Пути поиска VPATH

# Не определён путь поиска «vpath».

# Не определён общий (переменная «VPATH») путь поиска.

# буферы strcache: 1 (0) / строк = 486 / хранилище = 6445 Б / сред = 13 Б
# текущий buf: размер = 8174 Б / используется = 6445 Б / кол-во = 486 / сред = 13 Б

# производительность strcache: поисков = 816 / найдено = 40%
# состояние хеш-таблицы:
# Загружено=486/8192=6%, Переформулировано=0, Противоречий=238/816=29%
# Печать базы данных Make завершена Wed Oct 22 12:05:32 2014

