\\\n<=>nothing
#комментарий, кроме директив define и команд (здесь интерпретатор командной строки решает, что есть комментарий)

ищет по пор€дку `GNUmakefile', `makefile' и `Makefile'. 
	`Makefile' - при ls выводитс€ в начале вместе с README
	`GNUmakefile'- если специфичен только дл€ GNU make
опции -f им€_файла или --file=им€_файла 
	если их несколько - эти файлы объедин€ютс€
если нету файла - пытаетс€ выполнить цель только не€вными правилами
главна€ цель - по умолчанию перва€, начинающа€с€ не с .

phony - абстрактный
	.PHONY : clean
	clean :
		-rm edit $(objects)
так не будет путаницы, если по€витс€ файл clean
и ошибки будут игнорироватьс€

не€вные правила
	command.o : defs.h command.h
	=
	xxx.o : xxx.h defs.h command.h
		cc -c xxx.c
при использовании не€вных правил
можно использовать 1 цель в нескольких местах, и в 1м месте - несколько целей

var = string
$(var)
	обычно objects, OBJECTS, objs, OBJS, obj, или OBJ

директивы
	многострочна€ переменна€
	include
	условные части
